[project]
name = "dataset-tsukuba"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["queria>=0.22.0", "dbt-core>=1.11.2", "dbt-duckdb>=1.10.0"]

[project.optional-dependencies]
convert = ["normalize-japanese-addresses"]
